import React from 'react';
import { StyledTestWrapper } from './TestComponent.styled';

function TestComponent() {
  return (
    <StyledTestWrapper>
      모두 화이팅!! 테스트 컴포넌트이므로 나중에 삭제해주세요~~~
    </StyledTestWrapper>
  );
}

export default TestComponent;
